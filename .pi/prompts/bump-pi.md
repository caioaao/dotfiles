---
description: Bump the pi Nix package to a new version
argument-hint: "<new-version>"
---
Read `nix-modules/shared/pi/README.md` and follow its upgrade instructions to bump pi to version `$@`. 

Commit the changes with the message "(pi): bump". Add any relevant info in the commit message.

Arguments: $@
