# Custom Slash Commands

This directory contains custom slash commands for Claude Code.

Create `.md` files here to define custom commands. Each file becomes a command:
- `commit.md` → `/commit`
- `review-pr.md` → `/review-pr`

See Claude Code documentation for command syntax.
